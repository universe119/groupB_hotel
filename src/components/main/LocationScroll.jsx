// import useLocation from "../../hooks/useLocation"; // useLocation 가져오기
// import Layout from "../common/Layout";

export default function LocationScroll() {
	// 	// useLocation 호출 시 Traffic과 Roadview 관련 인자는 사용하지 않음
	// 	const { ref_mapFrame } = useLocation(); // 기본값으로 위치 데이터만 사용
	// 	return (
	// 		<Layout title="LocationScroll">
	// 			{/* Layout으로 Map 페이지 레이아웃 설정 */}
	// 			<section className="locationScroll">
	// 				<h2>Location</h2>
	// 				{/* 맵 프레임 */}
	// 				<figure className="mapFrame" ref={ref_mapFrame}></figure>
	// 			</section>
	// 		</Layout>
	// 	);
}
